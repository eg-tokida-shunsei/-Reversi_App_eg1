<?php
#　プレイヤー変更プログラム
  $player_W = 1;
  $player_B = -1;

  for ($turn=1 ; $turn<40 ; $turn++){
    if($turn % 2 == 1){
      $player = $player_W;
    }
    else{
      $player = $player_B;
    }
    print($player);
  }